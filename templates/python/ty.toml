# intendant-template: v1
# Reference configuration for `ty` (Astral type-checker).
# Check the exact syntax on https://github.com/astral-sh/ty for the
# version in use — the API may evolve pre-1.0 (see ADR-0003).

[src]
include = ["src", "tests"]

[rules]
strict = true
