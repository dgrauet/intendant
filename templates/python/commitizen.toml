# intendant-template: v1
# Standalone commitizen config — prefer the [tool.commitizen] section
# in pyproject.toml.
[tool.commitizen]
name = "cz_conventional_commits"
version_provider = "pep621"
update_changelog_on_bump = false
