# intendant-template: v1
[intendant]
version = "1"
stack = "{{ stack }}"
enforcement = "advisory"            # "strict" | "recommended" | "advisory"

[exemptions]
# Example:
# LO001 = "Upstream fork with a flat layout, refactor planned for V2"
# CI003 = { reason = "Private repo, conventional commits not required", until = "2026-12-31" }
