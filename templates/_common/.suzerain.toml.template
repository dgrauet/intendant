# suzerain-template: v1
[suzerain]
version = "1"
stack = "{{ stack }}"
mode = "advisory"            # "strict" | "recommended" | "advisory"

[exemptions]
# Exemple :
# LO001 = "Fork upstream avec layout flat, refactor en V2"
# CI003 = { reason = "Repo privé, conv. commits non requis", until = "2026-12-31" }
